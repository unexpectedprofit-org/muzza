"use strict"

describe "Controller: PlaylistCtrl", ->

  it "should attach a list of pets to the scope", ->
    console.log "Test test"
    assert.equal(-1, [1,2,3].indexOf(5));
    assert.equal(-1, [1,2,3].indexOf(2));